name: 'Setup Flutter'
description: 'Checkout code + setup Flutter (with caching). Reusable across repositories.'

inputs:
  flutter-channel:
    description: 'Flutter channel or version (stable, beta, master, 3.24.x, etc.)'
    required: true
    default: 'stable'

  cache:
    description: 'Cache the flutter sdk and the pub cache across runs'
    required: false
    default: 'true'

  pub-cache:
    description: 'Cache the pub cache across runs (true/false, defaults to cache)'
    required: false
    default: ''

  cache-key-prefix:
    description: 'Prefix for cache key'
    required: false
    default: 'flutter'

  working-directory:
    description: 'Directory where flutter pub get should run'
    required: false
    default: '.'

  checkout-token:
    description: 'Token to use for checkout (useful for private repos)'
    required: false
    default: ${{ github.token }}

runs:
  using: 'composite'
  steps:
    - name: Checkout code
      uses: actions/checkout@v7
      with:
        token: ${{ inputs.checkout-token }}

    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        channel: ${{ inputs.flutter-channel }}
        cache: ${{ inputs.cache }}
        cache-key: ${{ inputs.cache-key-prefix }}-${{ inputs.flutter-channel }}
        pub-cache: ${{ inputs.pub-cache }}
