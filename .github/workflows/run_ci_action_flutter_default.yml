name: Run CI action flutter default
on:
  push:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * 0'  # every sunday at midnight

jobs:
  test:
    name: Run ci default
    runs-on: ubuntu-latest
    steps:
      - name: Run ci flutter
        uses: tekartik/ci.dart/.github/actions/run_ci_flutter@v1
